import React from 'react';

// 定義 Dashboard2 組件，作為 Dashboard 2 頁面的內容
const Dashboard2 = () => {
    return (
        <div>
            This is the Dashboard 2 page {/* 顯示 Dashboard 2 的頁面訊息 */}
        </div>
    );
}

// 將 Dashboard2 組件匯出，以便在其他模組中使用
export default Dashboard2;
