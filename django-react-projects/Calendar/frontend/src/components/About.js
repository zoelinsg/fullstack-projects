import React from 'react'; // 從 React 模組匯入 React 物件

// 定義 About 功能組件
// 這個組件用於顯示關於 (About) 頁面內容
const About = () => {
  return (
    <div>
      這是關於頁面
    </div>
  );
}

// 匯出 About 組件，讓其他模組可以引入這個組件使用
export default About;
